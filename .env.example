# Copy this to .env.local for local `vercel dev` testing (never commit the real file).
# In production, set these in Vercel: Project Settings -> Environment Variables.
GMAIL_USER=info@startuplinkunimelb.net
GMAIL_APP_PASSWORD=
